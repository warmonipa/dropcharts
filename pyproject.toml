[project]
name = "dropcharts-tools"
version = "1.0.0"
description = "Data generation and validation tools for PSO drop charts"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12,<5",
    "requests>=2.31,<3",
]

[tool.ruff]
line-length = 100
target-version = "py310"
